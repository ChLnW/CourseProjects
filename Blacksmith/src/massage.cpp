#include "massage.h"
#include "memory.h"
#include <err.h>

/* static int fd_file; */

/**
 * Create a file and let it take the physical page that data_ptr is currently
 * associated with.
 */
/* A5 TASK 3 */
/* void map_file(void *data_ptr) { } */

/**
 * Spray page tables to the file so that one page table recycles the page that `data_ptr`
 * currently associates with.
 */
/* A5 TASK 4 */
/* void map_pt(void *data_ptr) { } */
